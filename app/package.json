{
  "name": "travel-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npx serve -s public",
    "build": "esbuild src/index.js --bundle --outfile=public/bundle.js --loader:.js=jsx"
  },
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.11"
  }
}
